/**
 * Configuration for item insertion behavior across admin sections.
 * When true, new items are inserted at the top of the list.
 * When false, new items are appended to the bottom of the list.
 */
export const INSERTION_CONFIG = {
    skills: false,
    projects: false,
    experiences: true,
    educations: true,
    certifications: false,
    achievements: false
};

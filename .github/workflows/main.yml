name: Dummy Pass

on:
  push: {}
  pull_request: {}
  workflow_dispatch:

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Dummy pass
        run: |
          echo "Dummy pass - CI placeholder"
          # Always succeed
          exit 0

